# AI 영어 전화 학습 앱 - 프론트엔드 개발 가이드

> **프론트엔드 개발자를 위한 종합 가이드**
> 버전: 1.0 | 최종 수정일: 2026-01-12

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [파일 구조](#2-파일-구조)
3. [개발 환경 설정](#3-개발-환경-설정)
4. [기능별 상세 가이드](#4-기능별-상세-가이드)
5. [컴포넌트 명세](#5-컴포넌트-명세)
6. [API 사용법](#6-api-사용법)
7. [스타일 가이드](#7-스타일-가이드)
8. [개발 체크리스트](#8-개발-체크리스트)

---

## 1. 프로젝트 개요

### 1.1 앱 소개

AI 영어 전화 학습 앱은 사용자가 AI 튜터와 영어로 전화 통화하듯 대화를 연습할 수 있는 모바일 앱입니다.

### 1.2 기술 스택

| 분류 | 기술 |
|------|------|
| **프레임워크** | React 19 + Vite 7 |
| **라우팅** | React Router v7 |
| **스타일링** | CSS-in-JS (인라인 스타일) |
| **아이콘** | Lucide React |
| **모바일** | Capacitor (iOS/Android) |
| **백엔드** | AWS Lambda + API Gateway |
| **AI** | Claude (via Anthropic API) |
| **음성** | AWS Polly (TTS), Web Speech API (STT) |

### 1.3 주요 기능

```
┌─────────────────────────────────────────────────────────┐
│                    AI 영어 전화 앱                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. AI 전화 (핵심 기능)                                  │
│     └── AI 튜터와 실시간 영어 음성 대화                   │
│                                                         │
│  2. 맞춤 설정                                           │
│     ├── 억양 선택 (미국/영국/호주/인도)                   │
│     ├── 성별 선택 (남성/여성)                            │
│     ├── 말하기 속도 (느리게/보통/빠르게)                  │
│     ├── 난이도 (초급/중급/고급)                          │
│     └── 대화 주제 (비즈니스/일상/여행/면접)               │
│                                                         │
│  3. 통화 결과                                           │
│     ├── 말한 단어 수                                    │
│     ├── 대화 시간                                       │
│     └── 피드백                                          │
│                                                         │
│  4. AI 분석 (150단어 이상 시)                            │
│     ├── CAFP 점수 (복잡성/정확성/유창성/발음)             │
│     ├── 필러워드 분석                                   │
│     ├── 문법 교정                                       │
│     └── 어휘 분석                                       │
│                                                         │
│  5. 핵심 표현 연습                                       │
│     └── 교정된 표현 따라 말하기 연습                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 파일 구조

### 2.1 전체 구조

```
eng-learning/
│
├── 📁 src/                      # 소스 코드
│   ├── 📄 main.jsx              # 앱 진입점
│   ├── 📄 App.jsx               # 라우터 설정
│   ├── 📄 App.css               # 전역 스타일
│   ├── 📄 index.css             # 기본 스타일 리셋
│   │
│   ├── 📁 pages/                # 페이지 컴포넌트
│   │   ├── 📄 Home.jsx          # 홈 화면 (튜터 카드, 전화하기)
│   │   ├── 📄 Call.jsx          # 통화 화면 (음성 대화)
│   │   ├── 📄 Result.jsx        # 결과 화면 (통화 종료 후)
│   │   ├── 📄 Settings.jsx      # 설정 화면 (튜터 커스터마이즈)
│   │   ├── 📄 Analysis.jsx      # AI 분석 화면 (CAFP 점수)
│   │   └── 📄 Practice.jsx      # 표현 연습 화면 (따라 말하기)
│   │
│   ├── 📁 components/           # 재사용 컴포넌트
│   │   ├── 📄 index.js          # 컴포넌트 내보내기
│   │   ├── 📄 BottomNav.jsx     # 하단 네비게이션 바
│   │   └── 📄 TutorAvatar.jsx   # 튜터 아바타 (이니셜)
│   │
│   ├── 📁 utils/                # 유틸리티 함수
│   │   ├── 📄 api.js            # API 통신 (채팅, TTS, STT, 분석)
│   │   └── 📄 helpers.js        # 헬퍼 함수 (포맷팅, 스토리지)
│   │
│   ├── 📁 constants/            # 상수 정의
│   │   └── 📄 index.js          # 설정 옵션, 색상, API URL
│   │
│   ├── 📁 hooks/                # 커스텀 훅 (TODO: 구현 필요)
│   │   └── (empty)
│   │
│   └── 📁 assets/               # 정적 자산
│       └── (images, fonts...)
│
├── 📁 backend/                  # Lambda 함수 코드
│   └── 📄 lambda_function.py    # API 핸들러
│
├── 📁 docs/                     # 문서
│   ├── 📄 UI_UX_SPECIFICATION.md
│   ├── 📄 CALL_HISTORY_TAB_SPEC.md
│   └── 📄 FRONTEND_GUIDE.md     # (이 문서)
│
├── 📁 android/                  # Capacitor Android
├── 📁 ios/                      # Capacitor iOS
│
├── 📄 package.json              # 의존성 정의
├── 📄 vite.config.js            # Vite 설정
├── 📄 capacitor.config.ts       # Capacitor 설정
└── 📄 README.md                 # 프로젝트 설명
```

### 2.2 페이지별 역할

| 페이지 | 경로 | 역할 | 주요 기능 |
|--------|------|------|----------|
| **Home** | `/` | 홈 화면 | 튜터 카드, 전화하기 버튼, 히스토리 |
| **Call** | `/call` | 통화 화면 | 음성 인식, AI 응답, 실시간 대화 |
| **Result** | `/result` | 결과 화면 | 통화 요약, 피드백 요청 |
| **Settings** | `/settings` | 설정 화면 | 억양/성별/속도/난이도/주제 선택 |
| **Analysis** | `/analysis` | 분석 화면 | CAFP 점수, 학습 추천 |
| **Practice** | `/practice` | 연습 화면 | 교정 표현 따라 말하기 |

### 2.3 파일 의존성 다이어그램

```
                           ┌─────────────┐
                           │   main.jsx  │
                           └──────┬──────┘
                                  │
                           ┌──────▼──────┐
                           │   App.jsx   │
                           │  (Router)   │
                           └──────┬──────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
   ┌────▼────┐               ┌────▼────┐               ┌────▼────┐
   │  pages/ │               │components│               │  utils/ │
   └────┬────┘               └────┬────┘               └────┬────┘
        │                         │                         │
   ┌────┴────────────┐      ┌─────┴─────┐           ┌───────┴───────┐
   │                 │      │           │           │               │
┌──▼──┐ ┌────┐ ┌────┐│   ┌──▼──┐   ┌───▼───┐   ┌───▼───┐       ┌───▼───┐
│Home │ │Call│ │... ││   │Tutor│   │Bottom │   │ api.js│       │helpers│
│.jsx │ │.jsx│ │    ││   │Avatar│   │  Nav  │   │       │       │  .js  │
└──┬──┘ └─┬──┘ └────┘│   └──┬──┘   └───────┘   └───┬───┘       └───┬───┘
   │      │          │      │                      │               │
   │      │          │      │                      │               │
   └──────┴──────────┴──────┴──────────────────────┴───────────────┘
                                  │
                           ┌──────▼──────┐
                           │ constants/  │
                           │  index.js   │
                           └─────────────┘
```

---

## 3. 개발 환경 설정

### 3.1 필수 요구사항

- Node.js 18+
- npm 또는 yarn
- (모바일 빌드 시) Android Studio / Xcode

### 3.2 설치 및 실행

```bash
# 1. 의존성 설치
npm install

# 2. 개발 서버 실행
npm run dev

# 3. 빌드
npm run build

# 4. 모바일 앱 빌드 (선택)
npx cap sync
npx cap open android  # Android Studio
npx cap open ios      # Xcode
```

### 3.3 환경 변수

현재 API URL은 `src/constants/index.js`에 하드코딩되어 있습니다.
프로덕션 배포 시 환경 변수로 분리 권장:

```javascript
// .env
VITE_API_URL=https://n4o7d3c14c.execute-api.us-east-1.amazonaws.com/prod/chat
```

---

## 4. 기능별 상세 가이드

### 4.1 홈 화면 (Home.jsx)

#### 담당 기능
- 튜터 카드 표시 (이름, 성격 태그, 국가/성별)
- "바로 전화하기" CTA 버튼
- 전화 히스토리 목록 (최근 10개)
- 하단 네비게이션 바

#### 주요 상태 (State)

```javascript
const [activeTab, setActiveTab] = useState('call')  // 상단 탭: call, settings, history
const [callHistory, setCallHistory] = useState([])   // 통화 기록 배열
const [settings, setSettings] = useState(DEFAULT_SETTINGS)  // 튜터 설정
```

#### 네비게이션

```javascript
// 전화하기 버튼 클릭
navigate('/call')

// 히스토리 항목 클릭
navigate('/result', { state: { callData: item } })
```

---

### 4.2 통화 화면 (Call.jsx)

#### 담당 기능
- AI 튜터 인사 메시지 (TTS 재생)
- 사용자 음성 인식 (STT)
- AI 응답 생성 및 재생
- 실시간 대화 트랜스크립트 표시
- 통화 종료 및 결과 페이지 이동

#### 주요 상태 (State)

```javascript
const [messages, setMessages] = useState([])        // 대화 히스토리
const [isListening, setIsListening] = useState(false)  // 음성 인식 중
const [isSpeaking, setIsSpeaking] = useState(false)    // AI 말하는 중
const [isLoading, setIsLoading] = useState(false)      // AI 응답 생성 중
const [callDuration, setCallDuration] = useState(0)    // 통화 시간 (초)
```

#### 음성 인식 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                        음성 인식 흐름                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 마이크 버튼 탭                                               │
│     └── setIsListening(true)                                   │
│                                                                 │
│  2. Web Speech API 시작                                         │
│     └── recognition.start()                                    │
│                                                                 │
│  3. 음성 인식 결과 수신                                          │
│     └── recognition.onresult → transcript                      │
│                                                                 │
│  4. 사용자 메시지 추가                                           │
│     └── setMessages([...messages, { role: 'user', ... }])      │
│                                                                 │
│  5. AI 응답 요청                                                 │
│     └── await sendMessage(messages)                            │
│                                                                 │
│  6. AI 응답 재생 (TTS)                                          │
│     └── await speakText(response.message)                      │
│                                                                 │
│  7. 대기 상태로 복귀                                             │
│     └── "마이크를 눌러 말하세요"                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 주요 함수

| 함수 | 역할 |
|------|------|
| `startListening()` | 음성 인식 시작 |
| `stopListening()` | 음성 인식 중지 |
| `handleUserMessage(text)` | 사용자 발화 처리 → AI 응답 요청 |
| `speakAIResponse(text)` | AI 응답 TTS 재생 |
| `endCall()` | 통화 종료 및 Result 페이지 이동 |

---

### 4.3 결과 화면 (Result.jsx)

#### 담당 기능
- 통화 완료 아이콘 및 메시지
- 통화 통계 (새로운 단어, 말한 단어, 대화 시간)
- AI 분석 요청 버튼 (150단어 이상일 때)
- 표현 확인하기 버튼
- 피드백 모달 (별점)

#### 주요 상태 (State)

```javascript
const [analysis, setAnalysis] = useState(null)         // AI 분석 결과
const [isAnalyzing, setIsAnalyzing] = useState(false)  // 분석 중 여부
const [showFeedback, setShowFeedback] = useState(true) // 피드백 모달 표시
const [rating, setRating] = useState(0)                // 별점 (1-5)
```

#### 조건부 렌더링

```javascript
// AI 분석 버튼 활성화 조건: 150단어 이상
const canAnalyze = totalWords >= 150

// 버튼 표시
{canAnalyze ? (
  <button onClick={handleAnalyze}>AI 분석 요청</button>
) : (
  <p>AI 분석을 받으려면 최소 150단어가 필요해요.</p>
)}
```

---

### 4.4 설정 화면 (Settings.jsx)

#### 담당 기능
- 억양 선택 (4가지)
- 성별 선택 (2가지)
- 말하기 속도 선택 (3가지)
- 난이도 선택 (3가지)
- 대화 주제 선택 (4가지)
- 설정 저장

#### 주요 상태 (State)

```javascript
const [settings, setSettings] = useState({
  accent: 'us',         // 'us' | 'uk' | 'au' | 'in'
  gender: 'female',     // 'female' | 'male'
  speed: 'normal',      // 'slow' | 'normal' | 'fast'
  level: 'intermediate', // 'beginner' | 'intermediate' | 'advanced'
  topic: 'business',    // 'business' | 'daily' | 'travel' | 'interview'
})
```

#### 옵션 데이터 소스

모든 옵션은 `src/constants/index.js`에서 가져옵니다:

```javascript
import { ACCENTS, GENDERS, SPEEDS, LEVELS, TOPICS } from '../constants'
```

---

### 4.5 AI 분석 화면 (Analysis.jsx)

#### 담당 기능
- CAFP 점수 표시 (Complexity, Accuracy, Fluency, Pronunciation)
- 추천 학습 영역 (필러워드, 문법 실수, 단어 반복)
- 상세 분석 결과

#### 데이터 구조

```javascript
// 분석 결과 객체
{
  cafp_scores: {
    complexity: 72,    // 0-100
    accuracy: 78,
    fluency: 70,
    pronunciation: 75,
  },
  fillers: {
    count: 3,
    words: ['um', 'uh', 'you know'],
    percentage: 2.5,
  },
  grammar_corrections: [
    {
      original: "I go to school yesterday",
      corrected: "I went to school yesterday",
      explanation: "과거 시제 사용 필요"
    }
  ],
  vocabulary: {
    total_words: 567,
    unique_words: 234,
    advanced_words: ['comprehensive', 'elaborate'],
  },
  overall_feedback: "전반적으로 좋은 대화였습니다..."
}
```

---

### 4.6 표현 연습 화면 (Practice.jsx)

#### 담당 기능
- Step 1: 교정된 표현 설명
- Step 2: 듣고 따라 말하기
- Step 3: 완료

#### 주요 상태 (State)

```javascript
const [currentStep, setCurrentStep] = useState(1)      // 현재 단계 (1-3)
const [currentIndex, setCurrentIndex] = useState(0)    // 현재 연습 중인 표현 인덱스
const [isRecording, setIsRecording] = useState(false)  // 녹음 중 여부
const [userRecording, setUserRecording] = useState(null) // 사용자 녹음
```

#### 단계별 플로우

```
Step 1 (설명)          Step 2 (연습)         Step 3 (완료)
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│                 │    │ ████████░░░░░░░ │    │                 │
│ 이 표현을 짧게   │    │                 │    │       ✓         │
│ 연습해볼게요.    │───▶│ 듣고 따라       │───▶│                 │
│                 │    │ 말해보세요.      │    │   잘했어요!      │
│ ┌─────────────┐ │    │                 │    │                 │
│ │교정된 문장   │ │    │ ┌─────────────┐ │    │ 다음 학습 활동을 │
│ └─────────────┘ │    │ │   문장      │ │    │ 진행해보세요.    │
│                 │    │ └─────────────┘ │    │                 │
│ ┌─────────────┐ │    │                 │    │                 │
│ │   설명      │ │    │ [듣기][발음]    │    │                 │
│ └─────────────┘ │    │                 │    │                 │
│                 │    │     ┌───┐       │    │                 │
│    [다음]       │    │     │🎤 │       │    │    [다음]       │
│                 │    │     └───┘       │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

---

## 5. 컴포넌트 명세

### 5.1 TutorAvatar

튜터 이니셜을 표시하는 원형 아바타 컴포넌트

```jsx
import { TutorAvatar } from '../components'

// Props
<TutorAvatar
  name="Gwen"           // 필수: 튜터 이름 (첫 글자가 표시됨)
  size="large"          // 선택: 'small' | 'medium' | 'large'
  backgroundColor="#8b5cf6"  // 선택: 배경색
  style={{}}            // 선택: 추가 스타일
/>

// 사이즈별 크기
// small:  48x48px,  font: 20px
// medium: 100x100px, font: 40px
// large:  140x140px, font: 56px, border 있음
```

### 5.2 BottomNav

하단 6탭 네비게이션 바

```jsx
import { BottomNav } from '../components'

// Props
<BottomNav
  activeTab="ai-call"   // 선택: 활성 탭 ID
  onTabChange={(tabId) => {}}  // 선택: 탭 변경 콜백
/>

// 탭 ID 목록
// 'home', 'lesson', 'ai-tutor', 'ai-call', 'achievement', 'my'
```

### 5.3 추가 구현 필요 컴포넌트

| 컴포넌트 | 용도 | 우선순위 |
|---------|------|---------|
| `MessageBubble` | 채팅 메시지 버블 | 높음 |
| `OptionGrid` | 설정 옵션 그리드 | 높음 |
| `ScoreCard` | CAFP 점수 카드 | 중간 |
| `ProgressBar` | 진행 바 | 중간 |
| `Modal` | 범용 모달 | 중간 |
| `CallHistoryCard` | 통화 기록 카드 | 중간 |
| `LoadingSpinner` | 로딩 인디케이터 | 낮음 |

---

## 6. API 사용법

### 6.1 API 모듈 구조

모든 API 호출은 `src/utils/api.js`를 통해 수행합니다.

```javascript
import {
  sendMessage,        // AI 채팅
  analyzeConversation, // 대화 분석
  textToSpeech,       // TTS (Polly)
  speechToText,       // STT (Transcribe)
  speakText,          // TTS 재생 (Polly + 폴백)
  playAudioBase64,    // Base64 오디오 재생
} from '../utils/api'
```

### 6.2 AI 채팅

```javascript
// 메시지 전송 및 AI 응답 받기
const response = await sendMessage([
  { role: 'user', content: 'Hello!' },
  { role: 'assistant', content: 'Hi! How are you?' },
  { role: 'user', content: "I'm doing great, thanks!" },
])

console.log(response.message) // AI 응답 텍스트
```

### 6.3 TTS (텍스트 → 음성)

```javascript
// 방법 1: 직접 TTS API 호출
const ttsResponse = await textToSpeech("Hello, how are you?")
await playAudioBase64(ttsResponse.audio)

// 방법 2: 자동 폴백 포함 (권장)
await speakText("Hello, how are you?", settings, audioRef)
// Polly 실패 시 자동으로 브라우저 TTS 사용
```

### 6.4 대화 분석

```javascript
// 대화 내용 분석 요청
const result = await analyzeConversation(messages)

console.log(result.analysis.cafp_scores)  // CAFP 점수
console.log(result.analysis.fillers)       // 필러워드
console.log(result.analysis.grammar_corrections)  // 문법 교정
```

### 6.5 API 에러 처리

```javascript
try {
  const response = await sendMessage(messages)
  // 성공 처리
} catch (error) {
  console.error('API 호출 실패:', error)
  // 사용자에게 에러 메시지 표시
  // 또는 폴백 로직 실행
}
```

---

## 7. 스타일 가이드

### 7.1 스타일링 방식

이 프로젝트는 **CSS-in-JS (인라인 스타일)** 방식을 사용합니다.

```jsx
// 스타일 정의
const styles = {
  container: {
    padding: 20,
    backgroundColor: '#f9fafb',
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#1f2937',
  },
}

// 사용
<div style={styles.container}>
  <h1 style={styles.title}>제목</h1>
</div>
```

### 7.2 색상 상수 사용

```javascript
import { COLORS } from '../constants'

// 사용
<div style={{ backgroundColor: COLORS.primary }}>
  Primary Color
</div>

// 사용 가능한 색상
COLORS.primary      // #5046e4 (메인 보라)
COLORS.purple       // #8b5cf6 (연보라)
COLORS.success      // #22c55e (초록)
COLORS.error        // #ef4444 (빨강)
COLORS.gray[100]    // #f3f4f6 (연회색)
COLORS.gray[800]    // #1f2937 (진회색)
```

### 7.3 반응형 디자인

- 최대 너비: `500px` (모바일 앱 기준)
- 중앙 정렬: `margin: '0 auto'`

```jsx
const styles = {
  container: {
    maxWidth: 500,
    margin: '0 auto',
    minHeight: '100vh',
  },
}
```

---

## 8. 개발 체크리스트

### 8.1 신규 페이지 추가 시

- [ ] `src/pages/`에 컴포넌트 파일 생성
- [ ] `src/App.jsx`에 Route 추가
- [ ] 필요한 상수는 `src/constants/index.js`에 추가
- [ ] API 호출이 필요하면 `src/utils/api.js` 함수 사용
- [ ] 공통 컴포넌트는 `src/components/`에서 import

### 8.2 신규 컴포넌트 추가 시

- [ ] `src/components/`에 파일 생성
- [ ] `src/components/index.js`에 export 추가
- [ ] JSDoc 주석으로 Props 문서화
- [ ] 스타일은 파일 하단에 `const styles = {}` 형식으로 정의

### 8.3 API 추가 시

- [ ] `src/utils/api.js`에 함수 추가
- [ ] API_ACTIONS에 새 액션 타입 추가
- [ ] JSDoc으로 파라미터/반환값 문서화
- [ ] 에러 처리 로직 포함

### 8.4 배포 전 체크리스트

- [ ] `npm run build` 성공 확인
- [ ] 콘솔 에러/경고 없음 확인
- [ ] API URL 환경 변수 확인
- [ ] 모바일 테스트 (Capacitor)
- [ ] 음성 인식/TTS 권한 테스트

---

## 참고 문서

- [UI/UX 명세서](./UI_UX_SPECIFICATION.md) - 전체 화면 디자인 스펙
- [전화내역 탭 명세서](./CALL_HISTORY_TAB_SPEC.md) - 전화내역 상세 UI
- [백엔드 API 문서](./BACKEND-API.md) - Lambda API 스펙

---

> 문서 작성일: 2026-01-12
> 문의: 프로젝트 담당자에게 연락
